#include "PosRotSize3D.h"

using namespace base;

PosRotSize3D::PosRotSize3D(Vector3& pos, Quaternion& rot, Vector3& scale)
	: pos(pos), rot(rot), scale(scale)
{
	//void
}

PosRotSize3D::~PosRotSize3D() {
	//void
}
