
template <typename T>
class IndexedList {pub dCtor(IndexedList);
asd;// extend list_dl and implement the extra logic for:
contains;
remove;
getEntry;

	pub explicit IndexedList();

	pub virtual ~IndexedList();
};
