#pragma once

#include <base/gh.h>
#include <worldGame3D/actionsSystem/action/IAction.h>

namespace worldGame3D {
class IEntityAction : virtual public IAction {
    // void

    pub ~IEntityAction() override = default;
};
};
