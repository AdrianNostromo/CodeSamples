package base.util;

/**
 * Created by Adrin on 7/30/2016.
 */
public class JavaUtil {
/*
    // Unable to configure the constructor using this method, need to use a helper class maybe.
    public static Constructor makeDeclaredConstructor(Class targetClass) {
        Constructor ctor;
        try {
            ctor = targetClass.getDeclaredConstructor(HashMap.class);
        }catch(NoSuchMethodException x) {
            throw new Error("Error occured.");
        }
        ctor.setAccessible(true);

        return ctor;
    }
*/
}
