package local.profile;

import base.launcher.ILocalDataHandler;
import local.app.AppLocal;

//The player profile handles persistent game information.
public class PlayerProfileDataLocal extends PlayerProfileDataLocalPlatforms {

    public PlayerProfileDataLocal(AppLocal app, ILocalDataHandler localDataHandler) {
        super(app, localDataHandler);

	    //void
    }

}
